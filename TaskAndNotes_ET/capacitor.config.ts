import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'proyectoTaskAndNotes005D.com',
  appName: 'TaskAndNotes',
  webDir: 'www'
};

export default config;
